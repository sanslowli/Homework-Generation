# 1. 일단 싹 다 무시해 (기본값: 차단)
*

# 2. 하지만 이 파일들은 꼭 챙겨 (앱 구동 필수품)
!*.py
!requirements.txt
!.gitignore

# 3. VIP 폴더 3개는 통째로 챙겨 (이 안의 이미지는 자동 통과됨)
!Syntax Pitching/**
!Syntax Only/**
!Syntax + Open-ended Question/**

# 4. [중요] VIP 폴더 안에 있더라도 이건 절대 가져가지 마
*.mp4
*.mov
*.zip
.DS_Store
service_key.json